# TenFingers - Production Environment Variables
# Kopiere diese Datei und benenne sie um zu .env
# Fülle alle Werte aus und halte sie GEHEIM!

# ====================================
# PostgreSQL Datenbank Konfiguration
# ====================================

DB_HOST=postgres
DB_PORT=5432
DB_NAME=tenfingers
DB_USER=tenfingers_user

# WICHTIG: Generiere ein sicheres Passwort!
# Verwende: openssl rand -base64 32
DB_PASSWORD=ERSETZE_MICH_MIT_SICHEREM_PASSWORT

# ====================================
# JWT Secret
# ====================================

# WICHTIG: Generiere einen langen, zufälligen String!
# Verwende: openssl rand -base64 64
JWT_SECRET=ERSETZE_MICH_MIT_LANGEM_GEHEIMEN_JWT_STRING

# ====================================
# Server Konfiguration
# ====================================

PORT=4000
NODE_ENV=production

# ====================================
# Frontend URL
# ====================================

# Deine Domain (mit https://)
FRONTEND_URL=https://deine-domain.de

# ====================================
# React App Konfiguration (für Frontend)
# ====================================

# API URL (mit https://)
REACT_APP_API_URL=https://deine-domain.de/api

# ====================================
# Optional: E-Mail Konfiguration (für zukünftige Features)
# ====================================

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=deine-email@gmail.com
# SMTP_PASS=dein-app-passwort
# SMTP_FROM=noreply@deine-domain.de

# ====================================
# Sicherheitshinweise
# ====================================

# 1. Sichere Passwörter generieren:
#    openssl rand -base64 32
#
# 2. Niemals Credentials in Git committen!
#    .env ist bereits in .gitignore
#
# 3. Backup dieser Datei außerhalb des Servers!
#
# 4. Regelmäßig JWT_SECRET rotieren (alle 6-12 Monate)
#
# 5. Starke DB_PASSWORD verwenden (min. 32 Zeichen)
